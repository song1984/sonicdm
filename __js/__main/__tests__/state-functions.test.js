import Carousel from '../source/component/Carousel';
import React from 'react';
import { mount } from 'enzyme';

test('TodoComponent renders the text inside it', () => {
	let app = mount(<Carousel />);
	// app.find('button').simulate('click')
});